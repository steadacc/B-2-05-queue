{
  "name": "corleyevents-detect-image-daemon",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "serve": "docker-compose up -d",
    "test": "docker-compose run -e NODE_ENV=test --rm web ./node_modules/.bin/jasmine",
    "logs": "docker-compose logs -f --tail=10 web"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.258.1",
    "config": "^3.3.8",
    "nodemon": "^1.19.1",
    "pino": "^5.12.4",
    "q": "^1.5.1",
    "ramda": "^0.25.0",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "forever": "^0.15.3",
    "jasmine": "^3.3.0",
    "mock-require": "^3.0.2",
    "nock": "^10.0.2"
  }
}
